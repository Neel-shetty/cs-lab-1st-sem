#write a program to generate a set, list and tuple from the sequence from the input from the user
num = int(input("enter the numbers - "))
num = str(num)
print(set(num))
print(list(num))
print(tuple(num))
test = set
print(test)